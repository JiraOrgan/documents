---
name: project-doc-manager
description: 프로젝트 문서 폴더 구조와 템플릿을 생성하고 버전을 관리하는 스킬. 사용자가 프로젝트 문서 초기화, 문서 생성, 문서 템플릿, 프로젝트 구조 설정, 문서 버전 관리, 스케줄 문서, ERD 문서, API 정의서, 아키텍처 정의서, 요구사항 정의서, 화면 정의서, 스토리보드, 테스트 전략서, 코드 리뷰 규칙, 배포 가이드, Git 규칙 등을 언급하면 이 스킬을 사용한다. "/doc init", "/doc create", "프로젝트 문서 만들어줘", "문서 폴더 구조 잡아줘" 같은 요청에도 트리거된다. 문서 관련 요청이 조금이라도 있으면 반드시 이 스킬을 사용할 것.
---

# Project Document Manager

프로젝트에 필요한 13종 표준 문서의 폴더 구조를 생성하고, 각 문서의 기본 템플릿을 제공하며, 파일명 기반 버전 관리를 수행하는 스킬이다.

## 지원 문서 목록

| 번호 | 문서명 | 폴더명 | 설명 |
|------|--------|--------|------|
| 00 | 스케줄 | `00-스케줄` | 프로젝트 일정 및 마일스톤 |
| 01 | 프로젝트 계획서 | `01-프로젝트계획서` | 프로젝트 개요, 목표, 범위, 팀 구성 |
| 02 | ERD 문서 | `02-ERD` | Entity-Relationship 다이어그램 및 데이터 모델 |
| 03 | 아키텍처 정의서 | `03-아키텍처정의서` | 시스템 아키텍처, 기술 스택, 인프라 구성 |
| 04 | API 정의서 | `04-API정의서` | REST/GraphQL API 엔드포인트 명세 |
| 05 | 화면 흐름 시퀀스 다이어그램 | `05-화면흐름시퀀스` | 화면 간 이동 흐름 및 시퀀스 다이어그램 |
| 06 | 화면 기능 정의서 | `06-화면기능정의서` | 각 화면별 기능 상세 정의 |
| 07 | 요구사항 정의서 | `07-요구사항정의서` | 기능/비기능 요구사항 목록 |
| 08 | Git 규칙 정의서 | `08-Git규칙정의서` | 브랜치 전략, 커밋 컨벤션, PR 규칙 |
| 09 | 스토리보드 | `09-스토리보드` | 화면 단위 UI/UX 스토리보드 |
| 10 | 테스트 전략서 | `10-테스트전략서` | 테스트 범위, 방법론, 자동화 전략 |
| 11 | 코드 리뷰 규칙 | `11-코드리뷰규칙` | 코드 리뷰 기준, 체크리스트, 프로세스 |
| 12 | 배포 가이드 | `12-배포가이드` | 배포 환경, 절차, 롤백 전략 |

## 명령어

이 스킬은 자연어와 명령어 스타일 모두 지원한다.

### 1. 전체 초기화: `doc init`

프로젝트의 `docs/` 디렉토리 하위에 13개 문서 폴더와 각 폴더에 v1.0 기본 템플릿을 생성한다.

**자연어 예시**: "프로젝트 문서 초기화해줘", "문서 폴더 구조 잡아줘", "프로젝트 문서 셋업"

**동작**:
1. 프로젝트 루트에 `docs/` 디렉토리 생성
2. 13개 하위 폴더 생성 (위 테이블의 폴더명)
3. 각 폴더에 기본 템플릿 파일 생성 (예: `00-스케줄_v1.0.md`)
4. `docs/` 루트에 `README.md` (문서 인덱스) 생성

**실행 방법**: `references/templates.md`를 읽고 각 문서 유형별 템플릿을 사용하여 파일을 생성한다. 또는 `scripts/init_docs.sh` 스크립트를 실행한다.

### 2. 개별 문서 생성: `doc create <문서번호|문서명>`

특정 문서만 생성한다.

**자연어 예시**: "ERD 문서 만들어줘", "API 정의서 템플릿 생성해줘"

**동작**:
1. 해당 문서의 폴더가 없으면 생성
2. 기본 템플릿 파일을 v1.0으로 생성
3. 이미 존재하면 사용자에게 알림

### 3. 버전 업: `doc version <문서번호|문서명>`

기존 문서의 새 버전을 생성한다.

**자연어 예시**: "ERD 문서 버전 올려줘", "API 정의서 v2 만들어줘"

**동작**:
1. 해당 폴더에서 가장 최신 버전 파일을 찾음
2. 최신 버전을 복사하여 버전 번호를 +1.0 증가
3. 새 파일의 메타데이터(버전, 수정일) 업데이트
4. 변경 이력 섹션에 새 항목 추가

**버전 네이밍 규칙**:
- 파일명: `{번호}-{문서명}_v{Major}.{Minor}.md`
- Major: 큰 구조 변경 시 증가 (v1.0 → v2.0)
- Minor: 내용 수정 시 증가 (v1.0 → v1.1)
- 예시: `02-ERD_v1.0.md` → `02-ERD_v2.0.md`

### 4. 문서 목록 확인: `doc list`

현재 생성된 문서 목록과 각 문서의 최신 버전을 표시한다.

**자연어 예시**: "문서 현황 보여줘", "어떤 문서가 있어?"

### 5. 인덱스 갱신: `doc index`

`docs/README.md`의 문서 인덱스를 현재 폴더 상태에 맞게 갱신한다.

## 문서 포맷

모든 문서는 **Markdown + Mermaid** 혼합 포맷을 사용한다:
- 텍스트, 테이블, 체크리스트: Markdown
- 다이어그램 (ERD, 시퀀스, 플로우차트, 아키텍처): Mermaid 코드 블록

## 문서 메타데이터

모든 문서 상단에 다음 메타데이터를 포함한다:

```markdown
---
문서명: {문서 제목}
버전: v{Major}.{Minor}
작성일: {YYYY-MM-DD}
최종수정일: {YYYY-MM-DD}
작성자: {작성자}
상태: 초안 | 검토중 | 승인됨
---
```

## 변경 이력

모든 문서 하단에 변경 이력 테이블을 포함한다:

```markdown
## 변경 이력

| 버전 | 날짜 | 작성자 | 변경 내용 |
|------|------|--------|-----------|
| v1.0 | {YYYY-MM-DD} | {작성자} | 최초 작성 |
```

## 폴더 구조

```
docs/
├── README.md                          # 문서 인덱스
├── 00-스케줄/
│   └── 00-스케줄_v1.0.md
├── 01-프로젝트계획서/
│   └── 01-프로젝트계획서_v1.0.md
├── 02-ERD/
│   └── 02-ERD_v1.0.md
├── 03-아키텍처정의서/
│   └── 03-아키텍처정의서_v1.0.md
├── 04-API정의서/
│   └── 04-API정의서_v1.0.md
├── 05-화면흐름시퀀스/
│   └── 05-화면흐름시퀀스_v1.0.md
├── 06-화면기능정의서/
│   └── 06-화면기능정의서_v1.0.md
├── 07-요구사항정의서/
│   └── 07-요구사항정의서_v1.0.md
├── 08-Git규칙정의서/
│   └── 08-Git규칙정의서_v1.0.md
├── 09-스토리보드/
│   └── 09-스토리보드_v1.0.md
├── 10-테스트전략서/
│   └── 10-테스트전략서_v1.0.md
├── 11-코드리뷰규칙/
│   └── 11-코드리뷰규칙_v1.0.md
└── 12-배포가이드/
    └── 12-배포가이드_v1.0.md
```

## 실행 가이드

### 전체 초기화 시

1. `references/templates.md`를 읽어 각 문서 유형의 템플릿 내용을 확인
2. 프로젝트 루트 경로를 확인 (사용자에게 물어보거나 현재 작업 디렉토리 사용)
3. `scripts/init_docs.sh`를 실행하거나, 직접 폴더와 파일을 생성
4. 각 템플릿의 `{YYYY-MM-DD}` 플레이스홀더를 오늘 날짜로 치환
5. 각 템플릿의 `{프로젝트명}` 플레이스홀더를 프로젝트명으로 치환 (사용자에게 확인)
6. `docs/README.md` 인덱스 파일 생성

### 개별 문서 생성 시

1. 사용자가 요청한 문서 번호 또는 이름을 위 테이블에서 매칭
2. 해당 문서의 템플릿을 `references/templates.md`에서 읽기
3. 폴더 생성 후 템플릿 파일 생성
4. `docs/README.md` 인덱스 갱신

### 버전 업 시

1. 해당 문서 폴더에서 `ls`로 기존 파일 확인
2. 가장 높은 버전 번호의 파일을 복사
3. 버전 번호를 +1.0 (Major) 또는 +0.1 (Minor) 증가
4. 메타데이터의 버전, 최종수정일 갱신
5. 변경 이력 테이블에 새 행 추가

## 주의사항

- 이미 존재하는 파일을 덮어쓰지 않는다. 항상 사용자에게 확인 후 진행한다.
- 프로젝트명, 작성자 등 플레이스홀더는 첫 초기화 시 사용자에게 확인한다.
- Mermaid 다이어그램은 문서 특성에 맞는 유형을 사용한다 (ERD → erDiagram, 시퀀스 → sequenceDiagram, 아키텍처 → flowchart 등).
